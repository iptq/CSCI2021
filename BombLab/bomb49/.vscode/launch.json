{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "target": "./bomb",
            "cwd": "${workspaceRoot}",
            "terminal": "x-terminal-emulator"
        }
    ]
}